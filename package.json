{
  "name": "@voasx/p2p-video",
  "version": "1.0.0",
  "description": "基于 WebRTC、Rust 和 React 的一发多收摄像头共享应用",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "cargo xtask dev",
    "dev:server": "cargo run --package webrtc-camera-share-server",
    "dev:web": "bun run --cwd apps/web dev",
    "build": "cargo xtask build",
    "release": "cargo xtask release",
    "typecheck": "bun run --cwd apps/web typecheck",
    "lint": "bun run --cwd apps/web lint && cargo clippy --package webrtc-camera-share-server --all-targets -- -D warnings",
    "test": "bun run --cwd apps/web test && cargo test --package webrtc-camera-share-server",
    "verify": "cargo xtask verify"
  },
  "keywords": [
    "webrtc",
    "websocket",
    "rust",
    "react"
  ],
  "license": "MIT",
  "packageManager": "bun@1.3.14"
}
